{
  "name": "anchor-web-co-website",
  "version": "1.0.0",
  "description": "Anchor Web Co. website - static SPA",
  "scripts": {
    "prebuild": "node scripts/fetch-cms-posts.js",
    "build": "npm run build:css && node -e \"require('node:fs').rmSync('_site', { recursive: true, force: true })\" && npx @11ty/eleventy",
    "build:css": "tailwindcss -c tailwind.config.js -i ./src/css/tailwind.css -o ./src/css/tailwind-build.css --minify",
    "preserve": "npm run build:css",
    "serve": "node -e \"require('node:fs').rmSync('_site', { recursive: true, force: true })\" && npx @11ty/eleventy --serve",
    "test": "npm run test:redirects && npm run verify:build",
    "test:redirects": "node scripts/test-cloudfront-function.js",
    "verify:build": "node scripts/verify-build.js",
    "cms:hash-password": "node scripts/cms-password-hash.js",
    "cms:seed-posts": "node scripts/seed-cms-posts.js",
    "test:crawl": "node test-crawl.js",
    "monitor:sites": "node scripts/monitor-managed-sites.js",
    "verify:production": "node scripts/verify-production.js"
  },
  "keywords": [
    "website",
    "seo",
    "static"
  ],
  "author": "Anchor Web Co.",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1069.0",
    "@aws-sdk/lib-dynamodb": "^3.1069.0"
  }
}
